server:
  port: 8083

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Isaac51743/my-config.git
          search-paths: config-test1

          # when git repository is private
          username: Isaac51743
          password: ZhaoYueHang66
